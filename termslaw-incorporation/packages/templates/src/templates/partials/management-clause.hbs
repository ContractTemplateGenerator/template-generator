<section class="management">
  <h3>Management</h3>
  {{#if isManagerManaged}}
    <p>The Company shall be managed by one or more Managers. The initial Managers are:</p>
    <ul>
      {{#each managers}}
        <li>{{default this "[Manager Name]"}}</li>
      {{/each}}
    </ul>
  {{else}}
    <p>The Company shall be managed directly by its Members acting as a Member-managed limited liability company.</p>
  {{/if}}
</section>
