import IncorporationIntake from '../components/IncorporationIntake';

export default function IntakePage() {
  return (
    <main className="min-h-screen bg-slate-100 py-8 px-4">
      <IncorporationIntake variant="default" />
    </main>
  );
}
